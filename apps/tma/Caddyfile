{$APP_DOMAIN} {
	encode zstd gzip

	@backend path /api/* /health /webhook/*
	handle @backend {
		reverse_proxy backend:8000
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
